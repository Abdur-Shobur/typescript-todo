export const formHandler: HTMLElement | null =
  document.getElementById('formHandler');

export let inputField =
  document.querySelector<HTMLInputElement>('#input-field')!;

export const form = document.getElementById(
  'formHandler',
) as HTMLFormElement | null;

export const taskBox = document.getElementById('task-box') as HTMLElement;
